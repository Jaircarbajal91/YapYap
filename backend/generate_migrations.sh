npx sequelize-cli migration:generate --name user
npx sequelize-cli migration:generate --name image
npx sequelize-cli migration:generate --name message
npx sequelize-cli migration:generate --name direct-message
npx sequelize-cli migration:generate --name dm-member
npx sequelize-cli migration:generate --name server-member
npx sequelize-cli migration:generate --name server
npx sequelize-cli migration:generate --name channel
